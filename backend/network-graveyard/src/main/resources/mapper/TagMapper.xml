<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neu.mapper.TagMapper">
    <resultMap id="TagResultMap" type="com.neu.domain.Tag">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findAll" resultMap="TagResultMap">
        SELECT * FROM tags
    </select>

    <insert id="insert" parameterType="com.neu.domain.Tag">
        INSERT INTO tags (name, create_by, create_time, update_by, update_time)
        VALUES (#{name}, #{createBy}, #{createTime}, #{updateBy}, #{updateTime})
    </insert>
</mapper>